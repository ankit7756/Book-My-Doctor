<resources>
    <style name="Theme._BookMyDoctor" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@android:color/black</item> <!-- Darker primary for night -->
        <item name="colorPrimaryVariant">@android:color/darker_gray</item> <!-- Darker variant -->
        <item name="colorOnPrimary">@android:color/white</item> <!-- Keep white text -->
        <!-- Optional: Adjust background or other colors -->
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>